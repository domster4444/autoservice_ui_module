import React from "react";
import { render, fireEvent, screen } from "@testing-library/react";
import Login from "./Login";

describe("Login page", () => {
  test("render login form with phone number and password fields", () => {});

  test("submits form with phone number and password values when submit button is clicked", () => {});

  test("display forgot password link", () => {});
});
